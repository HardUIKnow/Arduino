/*
  Serial input output with interrupt routine
  The code is in real C

  Usage: the program start with a hello word string 
  coming from arduino @ 9600 baud configuration

  Each time the user send data it pull up the 
  interrupt service routine and print the data
  
  This example code is in the public domain.
 */